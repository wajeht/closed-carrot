{
  "name": "mariposa-tech-hub-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo not yet dev",
    "dev:ui": "vite",
    "dev:api": "vite",
    "build": "npm run build:api && npm run build:ui",
    "build:api": "echo api does not need to build!",
    "build:ui": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react": "^3.0.0",
    "vite": "^4.0.0"
  }
}
