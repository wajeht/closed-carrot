FROM node:19-alpine

WORKDIR /usr/src/app

COPY ./package.json ./
COPY ./package-lock.json ./

RUN apk add --no-cache bash \
 && npm update \
 && npm install \
 && npm cache clean --force

COPY ./ .

RUN npm run build

CMD ["npm", "run", "start"]
